<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hello, Customer!</h1>
    <p>
      Thanks for your order. Here is the total and each item in your order<br>
    </p>

    <% @order.line_items.each do |line_item| %>
      <% if product = Product.find_by(id: line_item.product_id) %>
        <div>
           <p><strong>Item: </strong><%= product.name %></p>
           <p><strong>Quantity:</strong> <%= line_item.quantity %></p>
           <p><strong>Total:</strong> <%= product.price * line_item.quantity %></p>
        </div>
      <% end %>
    <% end %>

  </body>
</html>